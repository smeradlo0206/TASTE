# 第 11 批：补齐合同单元测试

## 给 Codex 的提示词

完整读取总提示词 C12-C16，确认七个顶层合同均已实现。本批不增加新业务类型，只补齐合同层测试和一致性。

任务：

1. 检查五个枚举、六个内嵌结构、七个顶层合同是否都有最小合法测试。
2. 为七个顶层合同完成 `object -> dict -> JSON -> object` round-trip。
3. 补齐总提示词 C16 的所有非法数据用例。
4. 检查错误信息包含合同名和字段路径。
5. 检查未知字段、缺少必填字段、无时区 datetime、可变默认值和映射浅复制。
6. 检查 `__init__.py` 只导出稳定公共名称。
7. 删除重复测试代码，但不得做与本任务无关的重构。

只修改合同包和合同测试。全部合同测试通过后停止。
